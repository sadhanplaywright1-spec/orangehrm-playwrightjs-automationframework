{
  "name": "orangehrm-playwrightjs-framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean:reports": "node scripts/clean-reports.js",
    "pretest": "npm run clean:reports",
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "test:ci": "playwright test tests/ui tests/api --reporter=line,allure-playwright",
    "test:coverage": "playwright test tests/ui tests/api --coverage --reporter=line,allure-playwright",
    "coverage:report": "nyc report --reporter=text-summary --reporter=lcov --report-dir coverage",
    "allure:generate": "allure generate allure-results -o allure-report --clean",
    "allure:open": "allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.35.0",
    "@types/node": "^26.2.0",
    "ajv": "^8.20.0",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^2.15.1",
    "csv-parse": "^5.0.0",
    "dotenv": "^16.6.1",
    "nyc": "^17.1.0",
    "pino": "^8.21.0",
    "pino-pretty": "^13.1.3",
    "xlsx": "^0.18.5"
  }
}
