# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: dashboard.spec.js >> @smoke Verify Dashboard Page
- Location: tests\ui\dashboard.spec.js:6:1

# Error details

```
Test timeout of 60000ms exceeded.
```

```
Error: expect(locator).toHaveText(expected) failed

Locator: locator('h6.oxd-topbar-header-breadcrumb-module')
Expected: "Dashboard"
Error: element(s) not found

Call log:
  - Expect "toHaveText" with timeout 10000ms
  - waiting for locator('h6.oxd-topbar-header-breadcrumb-module')
  - 

```

# Test source

```ts
  1  | const apiLocator = require('../../src/utils/RetryLocator');
  2  | const { test, expect } = require('../../src/fixtures/CustomFixtures');
  3  | const LoginPage =
  4  | require('../../src/pages/LoginPage');                                                              
  5  | const env =require('../../config/environments/qa.json');
  6  | test('@smoke Verify Dashboard Page', async ({ page }) => {
  7  | const loginPage =
  8  | new LoginPage(page);
  9  | await page.goto(env.baseURL);
  10 | await loginPage.login(
  11 | env.username,
  12 | env.password
  13 | );
  14 | await expect(page)
  15 | await page.waitForURL(
  16 | '**/dashboard/index'
  17 | );
  18 | await expect(
  19 | page.locator(
  20 | 'h6.oxd-topbar-header-breadcrumb-module'
  21 | )
> 22 | ).toHaveText('Dashboard');
     |   ^ Error: expect(locator).toHaveText(expected) failed
  23 | });
```